name: Code-Rollout
on:
  push:
    branches: [ main ]
    paths:
      - sources/**
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    environment: T1
    steps:
      - uses: actions/checkout@main
      
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          enable-AzPSSession: true
      
      - name: Get Function App Publishing Profile
        uses: azure/powershell@v1
        id: webAppPublishingProfile
        with:
          inlineScript: |
            Get-AzWebAppPublishingProfile -ResourceGroupName ${{ secrets.ResourceGroupName }} -Name ${{ secrets.functionAppName }} -OutputFile "${{ secrets.functionAppName }}.publishsettings"
          azPSVersion: 'latest'

      - name: Listing files
        uses: azure/powershell@v1
        with:
          inlineScript: |
            Get-ChildItem
            Get-Item -Path Env:
          azPSVersion: 'latest'
      
      - name: 'Run Azure webapp deploy action using publish profile credentials'
        uses: azure/webapps-deploy@v1
        with:
          app-name: ${{ secrets.functionAppName }}
          publish-profile: "${{ secrets.functionAppName }}.publishsettings"
          package: sources/